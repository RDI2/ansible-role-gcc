---
# defaults file for gcc

gcc_version: 5.3.0
gcc_url: http://gcc.parentingamerica.com/releases/gcc-{{ gcc_version }}/gcc-{{ gcc_version }}.tar.bz2
gcc_prefix: /opt/gcc/{{ gcc_version }}

# make -j {{ gcc_make_jobs }}
gcc_make_jobs: 1
